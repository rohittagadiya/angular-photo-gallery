import { PhotoGridDirective } from './photo-grid.directive';

describe('PhotoGridDirective', () => {
  it('should create an instance', () => {
    const directive = new PhotoGridDirective();
    expect(directive).toBeTruthy();
  });
});
